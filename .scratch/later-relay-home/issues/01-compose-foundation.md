# Compose foundation and presentation model

Status: resolved

Implement the Compose build configuration, Relay theme tokens, tool-neutral Home presentation models, and isolated realistic sample state described by the parent spec.

Blocked by:

## Acceptance

- Compose is enabled with dependencies compatible with the current plugins and SDK.
- Theme and type treatments reflect the approved brand.
- State supports all specified agent and activity statuses without calendar-specific types.
- Sample data is clearly isolated from production state.

## Comments

- Implemented Compose configuration, brand theme, tool-neutral presentation models, and isolated Relay sample states.
